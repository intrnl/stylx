
/// Type helper function to make it easier for usage with TypeScript
/// We defer the creation of class names within the style definition,
/// until it's actually used.
export function create (def) {
	return def;
}
